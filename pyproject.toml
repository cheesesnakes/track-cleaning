[project]
name = "Fish-Classifier"
version = "0.1.0"
description = "Stream lined workflow for reviewing tracks from behaveAI and training species classification model"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.14.0",
    "torchvision>=0.29.0",
]
